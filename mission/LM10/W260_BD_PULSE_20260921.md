# W260 bounded BD pulse — H05 repair and shared uncertainty

Tracks #27; does not close the whole issue.

## Prepare / Prove / Commit

The current-code probe reproduced the three Historian findings: the BT kernel
accepted separated win graphs; the typed bridge truncated malformed records;
H05 workflow filters omitted direct kernel dependencies.

The repair checks directed strong connectivity before fitting unregularized BT,
validates original pair records, and includes both dependencies in CI filters.
Previous one-directional golden fixtures are now negative tests; positive fixtures
contain return wins. Strong connectivity establishes finite-MLE existence, not
numerical convergence or calibrated uncertainty; those remain BD-260.2 work.

BD-260.1 now supplies 90/95/99 normal criticals, normal mean intervals with known
population sigma, Student-t intervals with estimated variance, Fisher-z intervals,
percentile and BCa scalar IID bootstrap, Bonferroni/Holm adjusted p-values and a
seeded receipt contract. Constant/undefined bootstrap cases and perfect sample
correlation DEFER. Custom statistics require an explicit identifier. Confidence
sequences remain RESEARCH_TODO. Time-series/block and multivariate resampling
are outside this scalar contract.

BCa reports bias correction and jackknife acceleration, rejects singular
acceleration, extreme bias and invalid adjusted tails. It does not substitute
another method silently. Finite nondegenerate intervals still require statistical
assumption/coverage review before use on a new population.

Run `python -m pip install numpy==2.3.5 scipy==1.17.0` then
`python mission/LM10/w260_prove.py`. Local proof: six H05 regressions, nine
uncertainty tests, and two legacy script suites passed. Reference checks include
an analytic two-item BT probability, hand-calculated multiplicity examples,
tabulated quantiles, SciPy bootstrap parity and affine equivariance.

The workflow checks out the PR source SHA explicitly and emits a receipt with
source hashes, test outputs, environment and run identity. Local tests do not
constitute independent crew attestation or hosted proof. Merge remains a separate
review step. No engineering, acceptance or release credit is created.

## MIP and next BD

- Modernize: repair false-PASS and malformed-input paths.
- Innovate: add one shared uncertainty module, using SciPy's Student-t distribution
  rather than approximating its quantile with the normal critical.
- Perpetuate: retain deterministic tests, exact-source workflow, receipt and scope.
- Next: BD-260.2 BT uncertainty and BD-260.3 PCA subspace uncertainty, only after
  this shared runtime proof is accepted. Then common fixture → TRIAGE validation.

References: Hunter (2004), *MM algorithms for generalized Bradley–Terry models*,
https://doi.org/10.1214/aos/1079120141 ; SciPy bootstrap documentation,
https://docs.scipy.org/doc/scipy/reference/generated/scipy.stats.bootstrap.html .

The original QPS detailed-status publication remains independently blocked by
approval review; this generic provider patch does not publish that report.
